framework.properties: ADT框架的主配置文件，根据需要配置主要信息，也可以全部配置；

local.properties： 在本地跑脚本时，可用此文件做根据需要修改主配置文件中的值，需要修改的值只需在此文件中把对应的Key重新赋值即可；

192.168.234.129-chrome.properties： 此类的文件是利用Jenkins执行测试时，配置需要的信息，将该文件名在Jenkins上配置成参数的形式；